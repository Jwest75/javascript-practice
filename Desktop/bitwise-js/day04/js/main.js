// ------------ DAILY CHALLENGE ------------
// Prompt the user for two different numbers
let numOne = prompt("Please enter a number");
let numTwo =prompt("Please enter a second number");
console.log(typeOf(numOne));

// Convert the values to integers
// Store the two integers as variables
let intOne = parseInt(numOne);
let intTwo = parseInt(numTwo);
console.log(typeof(intOne));

// Compare the two numbers
let result=0;
if (intOne > intTwo)
{result=intTwo;

}
else if {
    result=intOne;
}
else{result = intOne
}
// Alert the larger number



















// ------------ TAKE HOME CHALLENGE ------------
// Add an edge case to your daily challenge code
// Check if the user input anything other than a number
// If so, prompt the user again to input a number
    

